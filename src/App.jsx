// src/App.jsx — Application root.
// Renders the React Router so the full route tree is reachable.
import AppRouter from "./router/AppRouter";

export default function App() {
  return <AppRouter />;
}
